import { FiltrohabitacionPipe } from './filtrohabitacion.pipe';

describe('FiltrohabitacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrohabitacionPipe();
    expect(pipe).toBeTruthy();
  });
});
