import { Filtrohabitacion2Pipe } from './filtrohabitacion2.pipe';

describe('Filtrohabitacion2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Filtrohabitacion2Pipe();
    expect(pipe).toBeTruthy();
  });
});
